[listen]
address = "127.0.0.1"
port = 5353

[upstream]
address = "8.8.8.8"
port = 53
timeout_ms = 5000

# Optional: local blocklist file (one domain per line)
# [blocklist]
# path = "blocklist.txt"

[feeds]
urlhaus = true
openphish = true
# phishtank_api_key = "your-api-key-here"
refresh_secs = 3600
